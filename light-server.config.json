{
    "serve": ".",
    "watchexps": [
        "**, !app/**/*.test.ts, !node_modules/**, !dist/** # npm run serve # reload",
        "!compiledJs/**, !compiledJsForTests/** !package.json, !package-lock.json # npm run serve # reload"
    ],
    "interval": 1000
}