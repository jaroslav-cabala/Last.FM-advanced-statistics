{
    "serve": ".",
    "watchexps": [
        "**, !**/*.test.ts, !node_modules/** # npm run serve # reload",
        "!dist/**, !compiledJs/**, !package.json, !package-lock.json # npm run serve # reload"
    ],
    "interval": 1000
}